# Agnes AI Python SDK

This package will provide the Python SDK for Agnes AI Chat, Image, and Video
APIs. The current milestone defines shared constants, configuration, errors, and
video compatibility helpers before implementing the HTTP client.

## Development

```bash
python -m pip install -e ".[dev]"
python -m pytest
```
